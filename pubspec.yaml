name: flutter_realtyflow_crm
description: Modern Gayrimenkul CRM Uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ✅ Localization - SDK'dan eklendi
  flutter_localizations:
    sdk: flutter

  # Web support
  universal_html: ^2.2.4

  # State Management
  provider: ^6.1.1

  # Networking
  dio: ^5.4.0
  retrofit: ^4.0.3
  json_annotation: ^4.9.0
  connectivity_plus: ^5.0.2
  analyzer: ^8.4.0
  google_fonts: ^6.2.1

  # Storage
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2

  # UI Components
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  fl_chart: ^0.66.0
  table_calendar: ^3.0.9
  image_picker: ^1.0.7

  # Utilities
  intl: ^0.20.2
  logger: ^2.0.2
  freezed_annotation: ^3.1.0
  equatable: ^2.0.5

  # Navigation
  go_router: ^13.0.0
  cupertino_icons: ^1.0.6

  # Firebase
  firebase_core: ^4.2.0
  firebase_messaging: ^16.0.3
  
  # Device Info
  device_info_plus: ^10.1.0

  # 🔥 Contract Yönetimi & Export İçin
  
  # URL Launcher (PDF Açma)
  url_launcher: ^6.2.4
  
  # Path Provider (Dosya Kaydetme)
  path_provider: ^2.1.2
  
  # Permission Handler (Dosya İzinleri)
  permission_handler: ^11.3.0
  
  # File Picker (Dosya Seçme)
  file_picker: ^10.3.3
  
  # CSV Export
  csv: ^6.0.0
  
  # Excel Export & DataGrid
  syncfusion_flutter_datagrid: ^26.1.35
  excel: ^2.1.0
  
  # PDF Operations
  flutter_pdfview: ^1.3.2
  pdf: ^3.10.4
  printing: ^5.11.0
  
  # 🔥 DOCX Export (YENİ)
  docx_template: ^0.4.0
  
  # Share & Open Files
  share_plus: ^7.2.2
  open_file: ^3.3.2
  open_filex: ^4.3.4
  archive: ^3.6.1
  
  # Functional Programming
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  json_serializable: ^6.7.1
  retrofit_generator: ^10.0.6
  freezed: ^3.2.3

# ============================================================
# 🎨 ASSETS CONFIGURATION
# ============================================================
flutter:
  uses-material-design: true

  # ============================================================
  # 📁 ASSETS KLASÖRLERI
  # ============================================================
  assets:
    # Images
    - assets/images/
    - assets/images/logo.png
    - assets/images/placeholder.png
    - assets/images/empty_state.png
    
    # Icons
    - assets/icons/
    
    # Templates (DOCX, PDF vb.)
    - assets/templates/
    - assets/templates/contract_template.docx
    - assets/templates/report_template.docx
    - assets/templates/invoice_template.docx
    
    # Fonts (Eğer custom font kullanıyorsanız)
    # - assets/fonts/
    
    # Localization (Eğer manuel JSON kullanıyorsanız)
    # - assets/locales/
    # - assets/locales/tr.json
    # - assets/locales/en.json

  # ============================================================
  # 🔤 CUSTOM FONTS (Opsiyonel)
  # ============================================================
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont-Regular.ttf
  #       - asset: assets/fonts/CustomFont-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/CustomFont-Italic.ttf
  #         style: italic
